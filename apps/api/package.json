{
    "name": "api",
    "version": "0.0.0",
    "scripts": {
        "dev": "uv run uvicorn api.asgi:app --reload --host 0.0.0.0 --port 8000",
        "start": "uv run uvicorn api.asgi:app --host 0.0.0.0 --port 8000",
        "generate:requirements": "uv pip compile pyproject.toml -o requirements.txt"
    }
}