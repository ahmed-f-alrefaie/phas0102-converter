{
    "name": "api",
    "version": "0.0.0",
    "scripts": {
        "dev": "uv run uvicorn api.asgi:app --reload --host",
        "generate:requirements": "uv pip compile pyproject.toml -o requirements.txt"
    }
}